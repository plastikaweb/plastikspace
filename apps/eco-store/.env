# PocketBase Configuration
# La URL es llegeix automàticament dels fitxers environment d'Angular
# Per defecte usa environment.staging.ts
# Per canviar: export POCKETBASE_ENV=development (o staging, production)

# Credencials per PocketBase LOCAL (development)
# Aquestes són les credencials que uses per entrar a http://127.0.0.1:8090/_/
POCKETBASE_DEV_ADMIN_EMAIL=test@test.com
POCKETBASE_DEV_ADMIN_PASSWORD=ChEN15!!!!

# Credencials per PocketBase REMOT (staging/production)
# Aquestes són les credencials que uses per entrar a https://eco-botiga.pockethost.io/_/
POCKETBASE_STAGING_ADMIN_EMAIL=info@plastikaweb.com
POCKETBASE_STAGING_ADMIN_PASSWORD=M!g8%JU#CKF4wW
