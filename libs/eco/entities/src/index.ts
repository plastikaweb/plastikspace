/**
 * Placeholder for the shared eco domain entities. AREF-01 replaces it with the
 * tenant, product, order, cart and cycle types promoted out of
 * `@plastik/eco-store/entities`.
 */
export type EcoEntitiesPlaceholder = never;
