/**
 * Placeholder for the role-partitioned `VIEW_CONFIG` provider consumed by
 * `@plastik/core/cms-layout` (AP-0 0.7). Nav items are conditional on role and
 * on tenant domain state, so this lib is `type:data-access`, not `type:util`.
 */
export type EcoAdminLayoutConfigPlaceholder = never;
