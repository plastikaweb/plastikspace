/**
 * Placeholder for admin-only entity types. Filled as the AP-1 to AP-3 modules
 * need them; anything both apps share belongs in `@plastik/eco/entities`.
 */
export type EcoAdminEntitiesPlaceholder = never;
